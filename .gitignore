# Special for this directory #
##############################
.jupyter/jupyter_nbconvert_config.json
  # this MUST be ignored because it encodes a hardpath for particular anaconda distributions
  # (not relative path) which seems to mess up the jupyterthemes font setup
  # the other json, jupyter_notebook_config.json, just tells jupyter to use nbextensions

# Deliberately ignored #
########################
.vim/plugged

# VIM extras #
##############
.session.*
.*.sw[a-z]
  # VERY IMPORTANT, need to install manually with :PlugInstall
  # on every server, otherwise everything fails

# R history #
#############
.Rhistory

# iPython history #
###################
.ipython/**/history.sqlite

# Storage #
###########
old
trash
learning
originals
copies
__trash__
__pycache__

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.log
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
