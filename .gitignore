# Specifically for this repository #
####################################
.vim/zcache
  # cache for the zotero integration plugin
latex/*/
  # ignore subdirectories; those are just sources/backups really
bin/cdo
  # compiled stuff; add to this list
empty*
  # practice files
*.log
  # random log files
tags
  # ctags-generated files
!.vim/doc/tags
  # put this one back
.vim/plugged
  # have issues with plugin directory if don't do this; may have to do with this directory
  # being git clones of other directories, and hard to sync objects in .git
.jupyter/jupyter_nbconvert_config.json
  # this MUST be ignored because it encodes a hardpath for particular anaconda distributions
  # (not relative path) which seems to mess up the jupyterthemes font setup
  # the other json, jupyter_notebook_config.json, just tells jupyter to use nbextensions
.jupyter/custom
  # because we want jupyterthemes to control this behavior completely; don't need
  # to sync this folder
.ipython/profile_default
  # don't sync this crap
learning
originals
  # originals are copies of previous versions

# vim extras #
##############
.session.vim
.vimsession
.vimtags
.*.sw[a-z]
  # very important, need to install manually with :pluginstall
  # on every server, otherwise everything fails

# R history #
#############
.Rhistory

# iPython history #
###################
.ipython/**/history.sqlite

# Temp files #
##############
*.tmp
.nfs*
  # .nfs files appear on Linux servers sometimes

# Storage #
###########
old
trash
copies
__pycache__

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases #
######################
*.sql
*.sqlite

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
