# Configuration file. The push-default allows "git push" to go to origin-master.
# See this link+comments for some ideas: https://coderwall.com/p/euwpig/a-better-git-log
# Also this thread: https://stackoverflow.com/q/2421011/4970632
[alias]
	leaf = !git log --graph --abbrev-commit --date=relative --branches --name-status --oneline --decorate --color $@
	tree = !git log --graph --abbrev-commit --date=relative --branches --name-status --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' $@
	leafs = !git log --graph --abbrev-commit --date=relative --branches --name-status --oneline --decorate --color --left-right $@
	trees = !git log --graph --abbrev-commit --date=relative --branches --name-status --left-right --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' $@
	quick = !git add -A && git commit
	dad = !curl https://icanhazdadjoke.com/ && echo && git add $@ # common typo
	wtf = !git-wtf
[push]
	default = current
[color]
	ui = on
