# Configuration file. The push-default allows "git push" to go to origin-master.
# See this link+comments for some ideas: https://coderwall.com/p/euwpig/a-better-git-log
# Also this thread: https://stackoverflow.com/q/2421011/4970632
# The 'update' command can be used e.g. if have untracked changes on one branch, but
# want to git fetch and git merge changes to a different file.
# amend = !git commit --amend -m \"$@\"
[alias]
	update = "!git stash clear; git stash; git merge $@; git stash pop"
	tree = !git log --graph --abbrev-commit --date=relative --branches --name-status --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' $@ 2>/dev/null
	twig = !git log --graph --abbrev-commit --date=relative --branches --name-status --oneline --decorate --color $@ 2>/dev/null
	leaf = !git log --graph --abbrev-commit --date=relative --branches --oneline --decorate --color $@ 2>/dev/null
	wtf = !git-wtf $@ 2>/dev/null
	stat = !git diff --stat $@ 2>/dev/null
[push]
	default = upstream
[color]
	ui = on
[credential]
	helper = store
[user]
	name = Luke Davis
	email = lukelbd@gmail.com
